

@{
    ViewBag.Title = "UserSiteMessage1";
    //Layout = null;
} 
              
 <span class="msg" style="font-family:Arial; font-size:inherit; color:red">@ViewBag.CustomMessage1</span>

@* <li><a href="@Url.Content("~/PublicSite/LogOff")">Logout</a></li>

         <script type ="text/javascript">
            window.setInterval(function () {
                UpdateUserSessionInfo();
            }, 1000 * 1 * 60);

            function UpdateUserSessionInfo() {
                var actionURL = '@Url.Action("UpdateTimeInterval", "PublicSite", new  { area = "UserSite"})';

                $.ajax({
                    url: actionURL,
                    type: 'post',
                    dataType: "json",
                    data: {
                        ReturnUrl: window.location.href
                    },
                    success: function (data) {
                    }
                });
            }
    </script>*@



