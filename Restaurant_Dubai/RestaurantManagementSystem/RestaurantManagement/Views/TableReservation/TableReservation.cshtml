@model IEnumerable<EntityLayer.TableReservation>

@{
    ViewBag.Title = "Table Reservation";
    //Layout = null;
}

<!-- Banner Section Start Here -->
<div class="inner-banner" style="top: 94px;">
    <img src="/images/banner/Table Reservation.JPG" alt="">
    <div class="title">
        <h1>Table Reservation</h1>
        <span><a href="#">Home</a> <i class="fa fa-bolt" aria-hidden="true"></i> <span class="last">Table Reservation</span></span>
    </div>
</div>
<!-- Banner Section end Here -->
<!-- Contact Section Start Here -->
<div class="contact-page-area spc-equal">
    <div class="container">
        <p>
            @Html.ActionLink("Create New", "AddTableReservation")
        </p>
        @using (Html.BeginForm("AddTableReservation", "TableReservation", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { role = "form" }))
        {
           
            <table class="table">
                <tr>
                    @*<th>
                        @Html.DisplayNameFor(model => model.TableNumberID)
                    </th>*@
                    <th>
                        @Html.DisplayNameFor(model => model.BookStartTime)
                    </th>
                    @*<th>
                        @Html.DisplayNameFor(model => model.BookEndTime)
                    </th>*@
                    <th>
                        @Html.DisplayNameFor(model => model.RestaurantName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CustomerName)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        @*<td>
                            @Html.DisplayFor(modelItem => item.TableNumberID)
                        </td>*@
                        <td>
                            @Html.DisplayFor(modelItem => item.BookStartTime)
                        </td>
                        @*<td>
                            @Html.DisplayFor(modelItem => item.BookEndTime)
                        </td>*@
                        <td>
                            @Html.DisplayFor(modelItem => item.RestaurantName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CustomerName)
                        </td>
                        @*<td>
                            @Html.DisplayFor(modelItem => item.ModifiedBy)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ModifiedDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreatedBy)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreatedDate)
                        </td>*@
                        <td>
                            <div>
                                <div style="float:left" title="Edit">
                                    <a href="@Url.Action("AddTableReservation", "TableReservation", new { id = item.TableReservationID })" class="btn btn-xs btn-info">
                                    <span class="glyphicon glyphicon-edit" aria-hidden="true">Edit</span>
                                    </a>
                                </div>
                                <div title="Delete">
                                    @using (Html.BeginForm("TableReservation", "TableReservation", new { id = item.TableReservationID }, FormMethod.Post))
                                    {
                                        @Html.HttpMethodOverride(HttpVerbs.Delete)
                                        <button type="submit" id="buttonsubmit" value="Delete"
                                                onclick="return confirm('Are you sure?');"
                                                class="btn btn-xs btn-danger">
                                            <span class="glyphicon glyphicon-trash">Delete</span>
                                        </button>
                                    }
                                </div>
                            </div>

                            @*@Html.ActionLink("Edit", "Edit", new { id = item.TableReservationID }) |
                                @Html.ActionLink("Details", "Details", new { id = item.TableReservationID }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.TableReservationID })*@
                        </td>
                    </tr>
                }
            </table>
        }
    </div>
</div>
<script type="text/javascript">
    $(function () {
        var message = '@TempData["NotifyMessage"]';
        var type = '@TempData["NotifyType"]';
        if (message != "")
            ShowNotify("", message, type);
    });
</script>